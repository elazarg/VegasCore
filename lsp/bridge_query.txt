You are an expert Lean 4 + Mathlib proof assistant. Write a complete, working Lean 4 proof.

Here are the relevant definitions and already-proved lemmas:

```lean
-- FDist = α →₀ ℚ≥0 (Finsupp)
abbrev FDist (α : Type) [DecidableEq α] := α →₀ ℚ≥0

noncomputable def FDist.totalWeight (d : FDist α) : ℚ≥0 := d.sum (fun _ w => w)

noncomputable def FDist.ofList (entries : List (α × ℚ≥0)) : FDist α :=
  entries.foldl (fun acc (a, w) => acc + Finsupp.single a w) 0

-- Already proved (converts Finsupp.sum over ofList to List.sum):
theorem FDist.ofList_sum_eq {α : Type} [DecidableEq α] {M : Type} [AddCommMonoid M]
    (entries : List (α × ℚ≥0))
    (g : α → ℚ≥0 → M)
    (h0 : ∀ a, g a 0 = 0)
    (hadd : ∀ a w₁ w₂, g a (w₁ + w₂) = g a w₁ + g a w₂) :
    (FDist.ofList entries).sum g =
    (entries.map (fun ⟨a, w⟩ => g a w)).sum

-- normalizedFinPMF definition:
noncomputable def normalizedFinPMF {n : Nat} (weights : Fin n → NNReal)
    (hn : 0 < n) : PMF (Fin n) :=
  let f : Fin n → ENNReal := fun i => ↑(weights i)
  have htop : ∑' i, f i ≠ ⊤ := by
    rw [tsum_fintype, ← ENNReal.coe_finset_sum]; exact ENNReal.coe_ne_top
  if h0 : ∑' i, f i = 0 then
    PMF.ofFintype (fun _ => (n : ENNReal)⁻¹) (...)
  else
    PMF.normalize f h0 htop

-- From Mathlib:
-- PMF.normalize_apply : (PMF.normalize f hf0 hf) a = f a * (∑' x, f x)⁻¹
-- tsum_fintype : ∑' (x : α), f x = ∑ x, f x  (for Fintype α)
-- ENNReal.inv_one : (1 : ENNReal)⁻¹ = 1
-- ENNReal.toReal_coe : (↑(r : NNReal) : ENNReal).toReal = (r : ℝ)
```

Prove:
```lean
theorem FDist.ofList_sum_eq_normalizedFinPMF_sum {α : Type} [DecidableEq α]
    (entries : List (α × ℚ≥0)) (hn : 0 < entries.length)
    (hnorm : (FDist.ofList entries).totalWeight = 1)
    (g : α → ℝ) :
    (FDist.ofList entries).sum (fun a w => (↑w : ℝ) * g a) =
    ∑ i : Fin entries.length,
      (normalizedFinPMF
        (fun i => ⟨(↑(entries.get i).2 : ℝ),
          by exact_mod_cast (entries.get i).2.coe_nonneg⟩) hn i).toReal *
      g (entries.get i).1 := by
  sorry
```

Strategy:
1. Use ofList_sum_eq to convert LHS to (entries.map (fun ⟨a, w⟩ => (↑w : ℝ) * g a)).sum
2. Convert List.sum to ∑ i : Fin n (by induction on entries or using List.sum_ofFn)
3. Show normalizedFinPMF returns raw weights when they sum to 1:
   - Compute totalWeight = sum of weights in list (using ofList_sum_eq with g = fun _ w => w)
   - Cast to ENNReal: the tsum of the NNReal weights = 1
   - So PMF.normalize_apply gives f i * 1⁻¹ = f i
   - toReal gives back the ℝ value

You may break into helper lemmas. Use sorry labeled -- CAST_TODO for clearly-true cast sub-goals if needed.
Provide actual Lean 4 syntax.