# Archived fused compiler material

This directory contains the former fused public-choice compiler and its
dependent proofs and fixtures. It is retained as readable research material
while individual arguments are re-expressed for the strict sealed-message
tower.

Nothing under this directory is an active Lean module or part of a build root.
The active compiler has one sealed rule per event-graph node; a source commit
and its reveal are separate protocol phases.
