import Vegas.Theorems.Progress.EventGraph
import Vegas.Theorems.Progress.CheckedProgram

/-!
# Progress Theorems

Topic index for progress facts.

* `Vegas.Theorems.Progress.EventGraph` contains facts that hold for every
  `EventGraph`.
* `Vegas.Theorems.Progress.CheckedProgram` contains facts that require the
  graph to come from checked `VegasCore` syntax.
-/
