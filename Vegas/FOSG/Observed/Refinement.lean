import Vegas.FOSG.Observed.Kernel

/-!
# Outcome-value refinement entrypoint

The outcome-value proof infrastructure lives in `Vegas.FOSG.Observed.Kernel`
so the canonical observed-program adequacy theorem uses it directly.  This
module remains as a focused import point for clients looking for the
refinement-facing API.
-/
